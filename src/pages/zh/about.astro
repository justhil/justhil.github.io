---
import { getEntry, render } from 'astro:content';
import PageLayout from '@/components/layouts/PageLayout.astro';
import '@/assets/style/blog.css';

const about = await getEntry('pages', 'about');
let Content = null;
if (about) {
  Content = (await render(about)).Content;
}
---

<PageLayout title={about?.data.title ?? '关于'} description={about?.data.description ?? ''} lang="zh">
  <div slot="header">
    <a href="/zh/" class="inline-flex items-center gap-1.5 text-sm text-slate9 transition-colors hover:text-slate12 cursor-pointer mb-12">
      <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M19 12H5M12 19l-7-7 7-7" /></svg>
      返回
    </a>
  </div>

  <div class="blog-content">
    <div class="mb-12">
      <h1>{about?.data.title ?? '关于'}</h1>
    </div>
    {Content ? <Content /> : <p>页面未找到</p>}
  </div>
</PageLayout>

<script src="//unpkg.com/heti/umd/heti-addon.min.js" is:inline></script>
<script>
  const heti = new Heti('.blog-content');
  heti.autoSpacing();
</script>
